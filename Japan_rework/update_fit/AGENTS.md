# update_fit Notes

- This directory is an adaptation workspace for vanilla update review, not live gameplay content.
- Treat `vanilla_pre_v1.19/` and `vanilla_v1.19/` as copied reference snapshots.
- Treat `mod_current/` as a copied reference snapshot of current mod overrides at setup time.
- Make actual gameplay edits in the live mod paths outside `update_fit/` unless the user explicitly asks to edit a copied reference.
- Keep reports and notes under `reports/`.

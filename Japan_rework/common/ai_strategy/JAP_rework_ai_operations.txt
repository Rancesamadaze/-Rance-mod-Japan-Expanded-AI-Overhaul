JAP_rework_operation_china_agency_push = {
	allowed = {
		original_tag = JAP
		has_dlc = "La Resistance"
	}

	enable = {
		OR = {
			has_government = neutrality
			has_government = fascism
		}
		country_exists = CHI
		CHI = {
			has_capitulated = no
			NOT = { is_ally_with = JAP }
			NOT = { is_subject_of = JAP }
		}
		num_finished_operations = {
			operation = operation_collaboration_government
			target = CHI
			value < 3
		}
		OR = {
			date > 1937.1.1
			has_war_with = CHI
			has_completed_focus = JAP_reinforce_the_beijing_garrison
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = intelligence_agency_usable_factories
		value = 20
	}

	ai_strategy = {
		type = intelligence_agency_branch_desire_factor
		id = branch_operation
		value = 500
	}

	ai_strategy = {
		type = intelligence_agency_branch_desire_factor
		id = branch_operative
		value = 500
	}
}

JAP_rework_operation_build_china_network = {
	allowed = {
		original_tag = JAP
		has_dlc = "La Resistance"
	}

	enable = {
		OR = {
			has_government = neutrality
			has_government = fascism
		}
		country_exists = CHI
		CHI = {
			has_capitulated = no
			NOT = { is_ally_with = JAP }
			NOT = { is_subject_of = JAP }
		}
		num_finished_operations = {
			operation = operation_collaboration_government
			target = CHI
			value < 4
		}
		OR = {
			date > 1937.1.1
			has_war_with = CHI
			has_completed_focus = JAP_reinforce_the_beijing_garrison
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = operative_mission
		mission = build_intel_network
		value = 2500
		mission_target = CHI
		num_operatives = 2
	}
}

JAP_rework_operation_collaboration_on_china = {
	allowed = {
		original_tag = JAP
		has_dlc = "La Resistance"
	}

	enable = {
		OR = {
			has_government = neutrality
			has_government = fascism
		}
		country_exists = CHI
		CHI = {
			has_capitulated = no
			NOT = { is_ally_with = JAP }
			NOT = { is_subject_of = JAP }
		}
		num_finished_operations = {
			operation = operation_collaboration_government
			target = CHI
			value < 2
		}
		OR = {
			date > 1937.1.1
			has_war_with = CHI
			has_completed_focus = JAP_reinforce_the_beijing_garrison
			is_preparing_operation = {
				operation = operation_collaboration_government
				target = CHI
			}
			is_running_operation = {
				operation = operation_collaboration_government
				target = CHI
			}
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = operative_operation
		operation = operation_collaboration_government
		value = 4000
		operation_target = CHI
	}

	ai_strategy = {
		type = operation_equipment_priority
		value = 100
	}

	ai_strategy = {
		type = operative_mission
		mission = build_intel_network
		value = 2000
		mission_target = CHI
	}
}

JAP_rework_operation_stalemate_collaboration_on_china = {
	allowed = {
		original_tag = JAP
		has_dlc = "La Resistance"
	}

	enable = {
		OR = {
			has_government = neutrality
			has_government = fascism
		}
		has_war_with = CHI
		country_exists = CHI
		CHI = {
			has_capitulated = no
			NOT = { is_ally_with = JAP }
			NOT = { is_subject_of = JAP }
		}
		num_finished_operations = {
			operation = operation_collaboration_government
			target = CHI
			value > 1
		}
		num_finished_operations = {
			operation = operation_collaboration_government
			target = CHI
			value < 4
		}
		OR = {
			date > 1941.1.1
			CHI = { surrender_progress > 0.45 }
			is_preparing_operation = {
				operation = operation_collaboration_government
				target = CHI
			}
			is_running_operation = {
				operation = operation_collaboration_government
				target = CHI
			}
		}
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = operative_operation
		operation = operation_collaboration_government
		value = 2500
		operation_target = CHI
	}

	ai_strategy = {
		type = operation_equipment_priority
		value = 100
	}

	ai_strategy = {
		type = operative_mission
		mission = build_intel_network
		value = 1500
		mission_target = CHI
	}
}

JAP_rework_operation_soviet_low_priority_before_northern_war = {
	allowed = {
		original_tag = JAP
		has_dlc = "La Resistance"
	}

	enable = {
		country_exists = SOV
		NOT = { has_war_with = SOV }
		NOT = { has_completed_focus = JAP_kantokuen }
		NOT = { has_completed_focus = JAP_prepare_for_war_with_the_soviet_union }
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = operative_mission
		mission = build_intel_network
		value = -3000
		mission_target = SOV
	}
}
